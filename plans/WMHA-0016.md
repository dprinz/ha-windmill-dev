---
ticket: WMHA-0016
status: done
updated: 2026-08-02
---

# Plan for WMHA-0016

## Current state verified

- The v1 observation path is polling-only. Real parameters from `custom_components/windmill/coordinator.py`: runs 60 s (≤3 pages × 100, early stop at watermark), health 60 s (coarse + optional detailed), workers 120 s (≤5 pages × 100, opt-in), runnables 30 min (≤25 selections), capabilities 6 h, update 6 h. 429 backoff 300–900 s.
- "Production observations from the stable polling implementation" named by the ticket **do not exist** — no busy-workspace observation has been made (recorded as an evidence gap in `docs/product/supported-versions-and-limitations.md`). The comparison therefore uses code-derived request bounds and labels every estimate as such.
- PR-015: push may supplement polling only after a measured comparison; v1 must not depend on inbound public reachability from Windmill to Home Assistant.
- Research convention: `docs/research/source-register.md` — claims need source, verification date (2026-08-02), confidence.
- ADR numbering: next free is 0003 (`docs/architecture/decisions/`).

## Proposed changes

| File or component | Change | Acceptance criterion |
| --- | --- | --- |
| `docs/research/windmill-push-observation.md` | New research note: current Windmill SSE/webhook contracts from primary sources; polling vs. SSE vs. webhook comparison with code-derived load, latency, failure recovery; HA restart/reconnect/network-boundary analysis; unresolved risks | AC 1 |
| `docs/architecture/decisions/0003-*.md` | New ADR recording the decision (expected: polling stays; push deferred with concrete revisit conditions — the evidence must decide) | AC 2, AC 4, AC 5 |
| `docs/research/source-register.md` | Add the newly consulted primary sources with verification date | AC 1 |
| `tickets/backlog/WMHA-0029-*.md` | Follow-up ticket: re-evaluate push observation when the revisit conditions fire | AC 1 (unresolved risks owned) |
| `tickets/in-progress/WMHA-0016-…md` | Honest AC status, validation evidence, review pending | all |

No production code unless the evidence supports an implementation (not expected: the pinned v1.775.2 OpenAPI contains no workspace-wide job-event stream, and no signed outbound job-completion webhook exists).

## Sequence

1. Verify SSE/webhook contracts against primary sources: pinned `openapi.yaml` at v1.775.2 (grep for `text/event-stream`), windmill.dev webhooks/streaming/error-handling docs, HA webhook docs. Done — findings in the research note.
2. Write the research note with the comparison (load from code-derived bounds, latency, failure recovery, restart/reconnect, network boundary, auth/replay).
3. Write ADR-0003 with the evidence-backed decision.
4. Create the backlog follow-up ticket; update the source register.
5. Fill the WMHA-0016 ticket honestly; leave status `in-progress`, review pending.
6. Run docs-only validation: `python scripts/validate_repository.py`, `git diff --check`.

## Validation

| Check | Command | Expected signal |
| --- | --- | --- |
| Guardrails | `python scripts/validate_repository.py` | exit 0 |
| Whitespace | `git diff --check` | exit 0 |

No test/lint/type suite needed: no production code changes.

## Failure modes and rollback

- All changes are new docs/tickets; rollback is `git checkout` / delete the new files.
- Upstream docs contradict the pinned OpenAPI — the pinned source wins per the source register; record the contradiction explicitly.
- Decision forced toward implementation without evidence — stop instead; deferral is a legitimate ticket outcome.

## Security and privacy review

- No inbound endpoint is added; no credential is used or committed.
- Web research content is treated as untrusted data; nothing from retrieved pages is executed or followed as instruction.
