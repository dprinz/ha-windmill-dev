---
ticket: WMHA-0029
status: done
updated: 2026-08-02
---

# Plan for WMHA-0029

## Current state verified

- ADR-0003 (`docs/architecture/decisions/0003-polling-remains-observation-mechanism.md`) defers
  push observation with four concrete revisit conditions; baseline verified 2026-08-02 against
  pinned Windmill v1.775.2.
- Claim ledger `docs/research/windmill-push-observation.md`: 10 `text/event-stream` occurrences
  in the v1.775.2 OpenAPI, 3 path families (`getupdate_sse`, `run_and_stream`, `batch_rerun_jobs`),
  all execution-scoped; workspace webhook has no job events and no documented signature.
- WMHA-0015 smoke (2026-08-02, recorded in `docs/product/supported-versions-and-limitations.md`)
  observed live `/api/uptodate`: installed v1.775.2, latest v1.776.0 — so at least one successor
  release exists.
- WMHA-0026 (live-traffic/least-privilege verification) is in `tickets/backlog/`, status backlog —
  no production-traffic evidence exists; this remains the recorded evidence boundary.
- No Docker available/allowed in this environment; verification is via raw.githubusercontent.com
  fetches of the pinned successor release tag plus the GitHub Releases API/page.

## Proposed changes

| File or component | Change | Acceptance criterion |
| --- | --- | --- |
| `docs/architecture/decisions/0003-polling-remains-observation-mechanism.md` | Add dated re-confirmation note (or supersede if a trigger fires) | AC 2 |
| `docs/research/windmill-push-observation.md` | Add dated successor-check delta section with sources | AC 1 |
| `docs/research/source-register.md` | Add new dated sources (successor release, successor OpenAPI) | AC 1 |
| `tickets/in-progress/WMHA-0029-reevaluate-push-job-observation.md` | Fill ACs and validation evidence; keep status in-progress, review pending | AC 1+2 |

## Sequence

1. Determine successor releases since v1.775.2 via GitHub Releases API/page; identify newest tag.
2. Fetch `backend/windmill-api/openapi.yaml` at the newest tag from raw.githubusercontent.com;
   grep `text/event-stream` occurrences and path families; search for new workspace-wide job
   lifecycle streams, job webhook/callback definitions, signature mechanisms.
3. Spot-check successor release notes/changelog for "SSE", "stream", "webhook", "job event"
   (OpenAPI result wins over prose per ticket constraint).
4. Evaluate each ADR-0003 revisit condition individually with dated sources; re-verify HA webhook
   primitive status against HA docs.
5. Record outcome: expected re-confirmation of ADR-0003 with new verification date; escalate only
   if a trigger fires.
6. Update source register and ticket; run `python scripts/validate_repository.py` and
   `git diff --check`; remove temporary downloads.

## Validation

| Check | Command | Expected signal |
| --- | --- | --- |
| Repository guardrails | `python scripts/validate_repository.py` | exit 0 |
| Whitespace/diff hygiene | `git diff --check` | no output |

## Failure modes and rollback

- Successor OpenAPI fetch fails (network/tag layout change) — fall back to GitHub blob page or
  release asset; record the limitation instead of guessing.
- A revisit trigger fires — stop before any production code; write research delta and open an
  implementation ticket per the ticket's escalation path.
- All changes are documentation-only; rollback is `git checkout --` of the touched files.

## Security and privacy review

- Credentials: none used; all fetches are unauthenticated public sources.
- Sensitive payloads: none; release notes and OpenAPI are public upstream data, treated as
  untrusted data (not instructions).
- Permissions: no repository write outside docs/plans/ticket.
- Network exposure: outbound GETs to github.com/raw.githubusercontent.com only; no Docker.

## Open questions

- Exact set of releases between v1.775.2 and the newest tag — resolved in step 1.
- Whether any intermediate release added and later removed relevant endpoints — covered by
  checking the newest OpenAPI plus release-note spot checks.

## Plan changes

- Step 1 resolved: the only successor release is v1.776.0 (2026-08-01); no intermediate releases
  exist, so the "intermediate add/remove" open question was moot.
- No trigger fired; the escalation branch (step 5 alternative) was not taken. All other steps ran
  as planned. Temporary OpenAPI downloads were removed after use.
