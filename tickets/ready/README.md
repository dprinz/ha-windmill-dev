# Ready

Tickets that satisfy the readiness criteria and can be activated. An agent
selects exactly one ticket from here, moves it to `in-progress/` and sets its
frontmatter status in the same change.
